# third_party

Pinned external source used by this project.

Expected use:

- Add `virtiofsd-rs` here only if the lab proves distro or TrueNAS packaged
  versions are insufficient.
- Keep local patches minimal and documented.
- Prefer upstreamable patches unless the change is deliberately specific to this
  single TrueNAS deployment.
