# deploy/addons/cni

Placeholder for optional CNI manifests or Helm values.

The v1 cluster can use k3s default flannel. Use this directory only if the
cluster desired state disables flannel and installs a replacement CNI.
